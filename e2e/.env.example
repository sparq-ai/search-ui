# Copy to e2e/.env (gitignored) to enable the live-store smoke test.
# Use a SEARCH-ONLY key — never an admin key.
SPARQ_APP_ID=your-app-id
SPARQ_API_KEY=your-search-only-key
SPARQ_COLLECTION=your-collection-id
